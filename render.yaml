services:
  - type: web
    name: web-crawler
    env: docker
    plan: free
    region: oregon
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8016
    buildFilter:
      paths:
        - src/**
        - Dockerfile
        - docker-compose.yml
        - render.yaml 